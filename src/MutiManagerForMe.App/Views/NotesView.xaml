<UserControl
    x:Class="MutiManagerForMe.App.Views.NotesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="32,28,32,32" RowSpacing="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <StackPanel>
                <TextBlock Text="Ghi chú" Style="{StaticResource PageTitle}" />
                <TextBlock Text="Lưu ý tưởng, nội dung họp và thông tin cần tra cứu." Foreground="{StaticResource TextMutedBrush}" />
            </StackPanel>
            <Button Content="＋ Ghi chú mới" Command="{Binding NewCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource Card}" Padding="10">
                <ListView ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedNote, Mode=TwoWay}" Background="Transparent">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" Margin="0,0,28,0" />
                                        <TextBlock Text="●" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Right" />
                                    </Grid>
                                    <TextBlock Text="{Binding Preview}" Style="{StaticResource MutedText}" Margin="0,6,0,8" MaxHeight="38" />
                                    <TextBlock Text="{Binding UpdatedAt}" Style="{StaticResource MutedText}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource Card}">
                <Grid RowSpacing="14">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBox Text="{Binding EditorTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="20" FontWeight="SemiBold" BorderThickness="0,0,0,1" Padding="0,4,0,12" />
                    <TextBox Grid.Row="1" Text="{Binding EditorContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap"
                             BorderThickness="0" Padding="0,16" ScrollViewer.VerticalScrollBarVisibility="Auto" />
                    <Grid Grid.Row="2">
                        <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                            <CheckBox Content="Ghim ghi chú" IsChecked="{Binding IsPinned, Mode=TwoWay}" />
                            <TextBlock Text="{Binding ValidationMessage}" Style="{StaticResource ValidationText}" VerticalAlignment="Center" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
                            <Button Content="Xóa" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButton}" />
                            <Button Content="Lưu ghi chú" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButton}" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
